requests>=2.31
pyyaml>=6.0
numpy>=1.26
# Note: v2 deliberately does NOT depend on rasterio/geopandas/shapely.
# SNODAS masked is a regular WGS84 lat/lon grid, so plain numpy + the math
# in src/trailsnow/snodas.py is enough. Adding rasterio later would be fine
# if we want to also handle reprojected outputs, but it's not needed now.
